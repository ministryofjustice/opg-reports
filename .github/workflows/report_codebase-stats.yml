name: "[Reports] Codebase Stats"
on:
  # pull_request:
  #   branches:
  #     - main

  # run at 03:30 on Monday, Wednesday, and Friday
  schedule:
    - cron: '30 3 * * 1,3,5'

# permissions
permissions:
  contents: write
  id-token: write
  actions: read
  pull-requests: none

jobs:
  # use the importer workflow to run everything
  run_codebases:
    name: "Run codebases stats command"
    uses: ./.github/workflows/_importer_github.yml
    secrets: inherit
    with:
      sub_cmd: "codebase-stats"
      upload_to_prod: false
  # an end step for cleaner workflow
  end:
    name: "End"
    runs-on: ubuntu-24.04
    needs:
      - run_codebases
      - run_codeowners
    steps:
      - run: |
          echo "done"
